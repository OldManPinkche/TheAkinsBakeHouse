{
  "scripts": {
    "build": "node scripts/build-site.js",
    "dev": "node scripts/dev-server.mjs",
    "env:setup": "node scripts/ensure-env.js",
    "check": "node --check assets/site.js && node --check assets/order-rules.js && node --check assets/config.js && node --check server/square-checkout.mjs && node --check netlify/functions/create-square-checkout.js && node --check cloudflare/square-checkout-worker.mjs && node --check scripts/build-site.js && node --check scripts/dev-server.mjs && node --check scripts/ensure-env.js",
    "worker:login": "wrangler login",
    "worker:secret": "wrangler secret put SQUARE_ACCESS_TOKEN",
    "worker:deploy": "wrangler deploy",
    "test": "node --test tests/*.test.mjs",
    "dev:netlify": "npm run build && netlify dev --dir outputs --functions netlify/functions --port 8888"
  },
  "devDependencies": {
    "jsdom": "^30.1.0",
    "netlify-cli": "^26.1.0",
    "wrangler": "^4.104.0"
  },
  "engines": {
    "node": "^22.22.2 || ^24.15.0 || >=26.0.0"
  }
}
